{% extends 'base.html.twig' %}

{% block title %}Edit Emails{% endblock %}

{% block body %}
    <h1>Edit Emails</h1>

    {{ include('emails/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('emails_index') }}">back to list</a>

    {{ include('emails/_delete_form.html.twig') }}
{% endblock %}
