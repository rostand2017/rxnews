{% extends 'layout.html.twig' %}

{% block title %}Benutzer {{ user.name }}{% endblock %}

{% block body %}
    <div class="container-fluid mb-4">
        <div class="how2 how2-cl4 flex-s-c mt-3">
            <h3 class="f1-m-2 cl3 tab01-title">
                <a href="{{ path('user_index') }}" class="text-dark"><i class="fa fa-step-backward"></i> Benutzer {{ user.name }}</a>
            </h3>
        </div>
        <table class="table">
            <tbody>
            <tr>
                <th>E-mail</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Datum hinzugefügt</th>
                <td>{{ user.createdat ? user.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            </tbody>
        </table>
        <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-outline-dark btn-block">Bearbeiten</a>

        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
